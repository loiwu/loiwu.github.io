---
layout: post
title:  "iOS 20 天"
date:   2016-4-8 9:15:00 +0800
published:  true
categories: my projects
---

iOS炼金术师 - 去粗取精

---

Part A - Links

---

(1)
[消息传递与消息转发](http://www.cnblogs.com/biosli/p/NSObject_inherit_2.html)

(2)
[IOS中的深拷贝和浅拷贝](http://www.cnblogs.com/liuzhi20101016/p/5095042.html)

(3)
[Grand Central Dispatch (GCD) 用法详细介绍](http://blog.csdn.net/mad2man/article/details/21786251)

(4)
[Objective-C isa 指针 与 runtime 机制](http://www.jianshu.com/p/41735c66dccb)

(5)
[初始化阶段 —— load 和 initialize](http://www.cnblogs.com/biosli/p/NSObject_inherit.html)

(6)
[Alloc 和 AllocWithZone 区别和联系](http://blog.csdn.net/jiajiayouba/article/details/44306679)

(7)
[iOS开发之ARC（自动引用计数）](http://www.oschina.net/translate/automatic-reference-counting-on-ios)

(8)
[iOS NSInvocation的学习](http://www.2cto.com/kf/201312/264291.html)

(9)
[JSPatch 实现原理详解](https://github.com/bang590/JSPatch/wiki/JSPatch-实现原理详解)

---

Part B - Efficiency - 摘自Effective Objective-C 2.0

---

Chapter 1 - Accustoming Yourself to Objective-C

---

Item 1 - Familiarize Yourself with Objective-C's Roots：
（1）作为C语言超集的Objective-C，加入了面向对象的特性。Objective-C采用了具有动态绑定特性的消息结构，对象的具体类型只有在运行时才会被决定。消息被接收后，究竟要执行怎样的代码，是由运行期环境而不是编译环境决定的。
（2）理解C语言的核心概念，特别是内存模型和指针，将有助于写出高效的Objective-C程序。

---

Item 2 - Minimize Importing Headers in Headers
（1）必要时才引入头文件。使用向前声明并在实现文件中引入头文件，可以降低类之间的耦合。
（2）比如要声明某个类遵循一项协议，此时无法使用向前声明，那么久把“该类遵循某协议”这条声明移到“class-continuation 分类”中。如果不行，就把协议单独放在一个头文件中，然后单独引入。

---

Item 3 - Prefer Listeral Syntax over the Equivalent Methods
（1）应尽量使用字面量语法来创建字符串、数值、数组、字典。预创建此类对象的常规方法相比，这么做会更加简明扼要。
（2）应尽量通过取下标操作来访问数组下标或字典中的键所对应的元素。
（3）用字面量语法创建数组或字典时，如果之中有nil，就会抛出异常。

---

Item 4 - Prefer Typed Constants to Preprocessor #define
（1）不要用预处理指令定义常量。因为这样定义出的常量不含有类型信息，编译器只会在编译前据此执行查找和替换操作。所以即使常量值被重新定义，编译器也不会产生警告信息，这会导致应用程序中的常量值不一致。
（2）在实现文件中使用static const定义“只在编译单元内可见的常量”。因为这类常量不在全局符号表中，所以不需要为其名称加前缀。
（3）在头文件中使用extern声明全局变量，并在相关实现文件中定义它的值。因为这种常量会出现在全局符号表中，所以其名称应该加以隔离，通常用与之相关的类名作为前缀。

---

Item 5 - Use Enumerations for States, Options, and Status Codes
（1）应该用枚举来表示状态机的状态、传递给方法的选项、状态码值，并给这些值起易懂的名字。
（2）如果把传递给某个方法的选项表示为枚举类型，而多个选项又可以同时使用，那么就将各个选项定义为2的幂，以便通过按位或操作将它们组合起来。
（3）用NS_ENUM与NS_OPTIONS宏定义枚举类型，并指明其底层数据类型。这样可以确保枚举是用开发者所选的底层数据类型实现出来的，而不会采用编译器所选的类型。
（4）在处理枚举类型的switch语句中，为了确保能正确处理所有分支，不要实现default分支。如果实现了default分支，在加入新枚举之后，编译器就会提示开发者：switch语句并未处理所有枚举。

---

Chapter 2 - Object, Messaging, and the Runtime

---

Item 6 - Understand Properties
（1）用@property语法来定义对象中所封装的数据。
（2）通过attribute来制定存储数据所需的正确语义。
（3）在设置属性所对应的实例变量时，要遵从该属性所声明的语义。
（4）在开发iOS程序时，应使用nonatomic属性，因为atomic属性会严重影响性能。

---

Item 7 - Access Instance Variables Primarily Directly When Accessing Them Internally
（1）在对象内部读取数据时，应该直接通过实例变量来读，而写入数据时，应该通过属性来写。
（2）在初始化方法和dealloc方法中，总是应该直接通过实例变量来读写数据。
（3）在使用惰性初始化技术配置某份数据时，需要通过属性来读取数据。

---

Item 8 - Understand Object Equality
（1）如果想检测对象的等同性，需要提供“isEqual:”和“hash”方法。
（2）相同的对象必须具有相同的hash码，但是两个hash码相同的对象不一定相同。
（3）不要盲目的逐个检测每条属性，而应该依照具体需求来制定检测方案。
（4）编写hash方法时，应该使用计算速度快并且哈希码碰撞率低的算法。

---

Item 9 - Use the Class Cluster Pattern to Hide Implementation Detail
（1）类族模式可以把实现细节隐藏在一套简单的公共接口后面。
（2）系统框架中经常使用类族。
（3）从类族的公共抽象基类中继承子类时要格外注意，若有开发文档，则应首先阅读。

--- 

Item 10 - Use Associated Objects to Attach Custom Data to Existing Classes
（1）可以通过“关联对象”机制把两个对象关联起来。
（2）定义关联对象时，可以指定内存管理语义，用以模仿定义属性时所采用的“拥有关系”和“非拥有关系”。
（3）只有在其他做法不可行时才应选用关联对象，因为这种做法通常会引入难以查找的bug。

---

Item 11 - Understand the Role of objc_msgSend
（1）消息由接收者、选择器、参数构成。给某个对象发送消息就是在该对象上调用方法。
（2）发给某对象的全部消息都要由动态消息派发系统来处理，该系统会查出对应的方法，并执行其代码。

---

Item 12 - Understand Message Forwarding
（1）如果对象无法响应某个选择子，则进入消息转发流程。
（2）通过运行期的动态方法解析功能，可以在需要用到某个方法时再将其加入到类中。
（3）对象可以把无法解读的某些选择子交给其它对象来处理。
（4）经过上述两步之后，如果还是没办法处理选择子，就启动完整的消息转发机制。

---

Item 13 - Consider Method Swizzling to Debug Opaque Methods
（1）在运行期，可以向类中新增或替换选择子所对应的方法实现。
（2）使用另一份实现来替换原有方法的实现，这道工序叫做“方法调配”，开发者常用此技术向原有视线中添加新功能。
（3）一般来说，只有调试程序的时候才需要在运行期修改方法实现，这种做法不宜滥用。

---

Item 14 - Understand What a Class Object Is
（1）每个实例都有一个指向Class对象的指针，用以表明其类型，而这些Class对象则构成了类的继承体系。
（2）如果对象类型无法再编译期确定，就应该使用类型信息查询方法来探知。
（3）尽量使用类型信息查询方法来确定对象类型，而不要直接比较类对象，因为某些对象可能实现了消息转发功能。

---

Chapter 3 - 接口与API设计

---

Item 15 - 用前缀避免命名空间冲突
（1）选择与你的公司、应用程序或者二者都有关联的名称作为类名的前缀，并在所有代码中均使用这一前缀。
（2）如果自己开发的程序中用到了第三方库，则应为其中的名称也加上前缀。

---

Item 16 - 提供“全能初始化方法”
（1）在类中提供一个全能初始化方法，并在文档中指明。其他初始化方法都应该调用此方法。
（2）如果全能初始化方法与超类的不同，那么就需要覆写超类中的对应方法。
（3）如果超类的初始化方法不适用于子类，那么久应该覆写这个超类方法，并在其中抛出异常。

Item 17 - 实现description方法
（1）实现description方法返回一个有意义的字符串，用以描述该实例。
（2）如果想在调试时打印出更详尽的对象描述信息，那么应该实现debugDescription方法。

Item 18 - 尽量使用不可变对象
（1）尽量创建不可变的对象。
（2）如果某属性仅可在对象内部修改，那么在“class-continuation分类”中将其由readonly属性扩展为readwrite属性。
（3）不要把可变的collection作为属性公开，而是应该提供相关方法，以此修改对象中的可变collection。

Item 19 - 使用清晰而协调的命名方式
（1）起名时应该遵从标准的Objective-C命名规范，这样创建出来的接口更容易为开发者所理解。
（2）方法名要言简意赅，从左至右读起来要像个日常用语中的句子才好。
（3）方法名里不要使用缩略后的类型名称。
（4）给方法起名时的第一要务是确保其风格与自己的代码或所要继承的框架相符。

-方法命名的注意事项
（1）如果方法返回值是新创建的，那么方法名的首个词应该是返回值的类型，除非签面还有修饰语，比如localizedString。属性的存取方法不遵循这种命名方式，因为一般认为这些方法不会创建新对象，即便有时返回内部对象的一份拷贝，也认为那相当于原有的对象。这些存取方法应该按照其所对应的属性来命名。
（2）应该把表示参数类型的名词放在参数前面。
（3）如果方法要在当前对象上执行操作，那么久应该包含动词；如果执行操作时还需要参数，那么应该在动词后面加上一个或多个名词。
（4）不要使用str这种简称，应该用string这样的全程。
（5）Boolean属性应加is前缀。如果某方法返回非属性的Boolean值，那么应该根据其功能，选用has或is当前缀。
（6）将get这个前缀留给那些借由“输出参数”来保存返回值的方法，比如，把返回值填充到“C语言式数组（C-style）”里的那种方法就可以使用get做前缀。

---

Item 20 - 为私有方法名加前缀
（1）给私有方法的名称加上前缀，这样可以很容易地将其与公共方法区分开。
（2）不要单用一个下划线做私有方法的前缀，因为这种做法是预留给苹果公司用的。

---

Item 21 - 理解Objective-C错误模型
（1）只有发生了可使整个应用程序泵扩的严重错误时，才应使用异常。
（2）在错误不那么严重的情况下，可以指派“委托方法（delegate method）”来处理错误，也可以把错误信息放在NSError对象里，经由“输出参数”返回给调用者。

---

Item 22 - 理解NSCopying协议
（1）若想另自己所写的对象具有拷贝功能，则需要实现NSCopying协议。
（2）如果自定义的对象分为可变版本和不可变版本，那么就要同事实现NSCopying和NSMutableCopying协议。
（3）复制对象时，需要决定采用浅拷贝还是深拷贝，一般情况下应该尽量执行浅拷贝。
（4）如果缩写的对象需要深拷贝，那么可以考虑新增一个专门执行深拷贝的方法。

---

Chapter 4 - 协议与分类

---

Item 23 - 通过委托与数据源协议进行对象间通信
（1）委托模式为对象提供了一套接口，使它们可以由此将相关事件告诉其他对象。
（2）将委托对象应该支持的接口定义成协议，在协议中把可能需要处理的时间定义成方法。
（3）当某对象需要从另一个对象中获取数据时，可以使用委托模式。这种情境下，该模式也成为“数据源协议”。
（4）如果必要，可以实现含有位段的结构体，将委托对象是否能响应相关协议方法这一信息缓存在其中。

---

Item 24 - 将类的实现代码分散到便于管理的数个分类之中
（1）使用分类机制把类的实现代码划分成易于管理的小块。
（2）将应该视为“私有”的方法归入名为Private的分类中，以隐藏实现细节。

---

Item 25 - 总是为第三方类的分类名称加前缀
（1）向第三方类中添加分类时，总应该给其名称加上你的专用前缀。
（2）向第三方类中添加分类时，总应该给其中的方法名加上你的专用前缀。

--- 

Item 26 - 不要在分类中声明属性
（1）把封装数据所用的全部属性都定义在主接口里。
（2）在“class-continuation分类”之外的其他分类中，可以定义存取方法，但尽量不要定义属性。

---

Item 27 - 使用“class-continuation分类”隐藏实现细节
（1）通过“class-continuation分类”向类中新增实例变量。
（2）如果某属性在主接口中声明为“只读”，而类的内部又要用设置方法修改此属性，那么就在“class-continuation分类”中将其扩展为“可读写”
（3）把私有方法的原型声明在“class-continuation分类”里面。
（4）如果想使类所遵循的协议不为人所知，那么可以在“class-continuation分类”中声明。

---

Item 28 - 通过协议提供匿名对象
（1）协议可在某种程度上提供匿名类型。具体的对象类型可以淡化成遵从某些以的id类型，协议里规定饿对象所应实现的方法。
（2）使用匿名对象来隐藏类型名称（或类名）。
（3）如果具体类型不重要，重要的是对象能都响应（定义在协议里的）特定方法，那么可以使用匿名对象来表示。

---

Chapter 5 - 内存管理

---

Item 29 - 理解引用计数
（1）引用计数机制通过可以递增递减的计数器来管理内存。对象创建好之后，其保留计数至少为1。如果保留计数为正数，那么对象继续存活。当保留计数降为0时，对象就被销毁。
（2）在对象声明周期中，其余对象通过引用来保留或释放此对象。保留与释放操作分别会递增和递减保留计数。

---

Item 30 - 以ARC简化引用计数
（1）有ARC之后，程序员就无须大新内存管理问题了。使用ARC来编程，可以省去类中的许多样板代码。
（2）ARC管理对象生命周期的基本办法是：在合适的地方插入“保留”及“释放”操作。在ARC环境下，变量的内存管理语义可以通过修饰符指明，而原来则需要手工执行“保留”及“释放”操作。
（3）由方法所返回的对象，其内存管理语义总是通过方法名来体现。ARC将此确定为开发者必须遵守的规则。
（4）ARC只负责管理Objective-C对象的内存。尤其要注意：CoreFoundation对象不归ARC管理，开发者必须适时调用CFRetain/CFRealease。

---

Item 31 - 在dealloc方法中只释放引用并接触监听
（1）在dealloc方法里，应该做的事情就是释放指向其他对象的引用，并取消原来订阅的“键值观察（KVO）”或NSNotificationCenter等通知，不要做其他事情。
（2）如果对象持有文件描述等系统资源，那么应该专门编写一个方法来释放此类资源。这样的类要和其使用者约定：用完资源后必须调用close方法。
（3）执行异步任务的方法不应该在dealloc里调用；只能在正常状态下执行的那些方法也不应该在dealloc里调用，因此此时对象已处于正在回收的状态。

---

Item 32 - 编写“异常安全代码”时留意内存管理问题
（1）捕获异常时，一定要注意try块内所创建的对象要清理干净。
（2）在默认情况下，ARC不生成安全处理异常所需的清理代码。开启编译器标识后（-fobjc_arc_exceptions），可生成这种代码，不过会导致应用程序变大，而且会减低运行效率。

---

Item 33 - 以弱引用避免保留环
（1）将某些引用设为weak， 可避免出现“保留环”。
（2）weak引用可以自动清空，也可以不自动清空。自动清空autoniling是随着ARC引入的新特性，由运行期系统来实现。在具备自动清空功能的弱引用上，可以随意读取其数据，因为这种引用不会指向已经回收过的对象。

---

Item 34 - 以“自动释放池”降低内存峰值
（1）自动释放池排布在栈中，对象收到autorelease消息后，系统将其放入最顶端的池里。
（2）合理运用自动释放池，可降低应用程序的内存峰值。
（3）@autoreleasepool这种新式写法能创建出更为轻便的自动释放池。

---

Item 35 - 用“僵尸对象”调试内存管理问题
（1）系统在回收对象时，可以不将其真的回收，而是把它转化为僵尸对象。通过环境变量NSZombieEnabled可以开启这项功能。
（2）系统会修改对象的isa指针，让它指向特殊的僵尸类，从而使该对象变味僵尸对象。僵尸类能够响应所有的选择子，响应方式为：打印一条包含消息内容及其接收者的信息，然后终止应用程序。

---

Item 36 - 不要使用retainCount
（1）对象的保留技术看似有用，实则不然，因为任何给定时间点上的“绝对保留计数”都无法反映对象生命期的全貌。
（2）引入ARC之后，retainCount方法就正式废止了，在ARC下调用该方法会导致编译器报错。

---

Chapter 6 - 块与大中枢派发

---

Item 37 - 理解“块”这一概念
（1）块是C、C++、Objective-C中的词法闭包。
（2）块可以接受参数，也可以返回值。
（3）块可以分配在栈或堆上，也可以是全局的。分配在栈上的块可以拷贝到堆里，这样就和标准的Objective-C对象一样，具备引用计数了。

---

Item 38 - 为常用的块类型创建typedef

---

Item 39 - 用handler块降低代码分散程度

---

Item 40 - 用块引用其所属对象时不要出现保留环

---

Item 41 - 多用派发队列，少用同步锁

---

Item 42 - 多用GCD，少用performSelector系列方法

---

Item 43 - 掌握GCD及操作队列的使用时机

---

Item 44 - 通过Dispatch Group机制，根据系统资源状况来执行任务

---

Item 45 - 使用dispatch_once来执行只需运行一次的线程安全代码

---

Item 46 - 不要使用dispatch_get_current_queue

---

Chapter 7 - 系统框架

---

Item 47 - 熟悉系统框架
（1）许多系统框架都可以直接使用。其中作重要的是Foundation和CoreFoundation，这两个框架提供了构建应用程序所需要的许多核心功能。
（2）许多常见任务都能用框架来做，例如音频与视频处理、网络通信、数据管理等。
（3）用纯C写成的框架与用Objective-C写成的框架一样重要，如果想成为优秀的Objective-C开发者，应该掌握C语言的核心概念。

---

Item 48 - 多用块枚举，少用for循环
（1）遍历collection的四种方式：最基本的办法是for循环，其次是NSEnumerator遍历法和快速遍历法（for in），最新、最先进的方式是“块枚举法”。
（2）“块枚举法”本身就能通过GCD来并发执行遍历操作，无须另行编写代码。而采用其他遍历方式则无法轻易实现这一点。
（3）如果提前知道待遍历的collection含有何种类型的对象，那么应该修改块签名，指出对象的具体类型。

---

Item 49 - 对定义其内存管理语义的collection使用无缝桥接 ？？？
（1）通过无缝桥接技术，可以在Foundation框架中的Objective-C对象与CoreFoundation框架中的C语言数据结构之间来回转换。
（2）在CoreFoundation层面创建collection时，可以指定许多回调函数，这些函数表示此collection应如何处理其元素。然后，可运用无缝桥接技术，将其转换成具备特殊内存管理语义的Objective-C collection。

---

Item 50 - 构建缓存时选用NSCahce而非NSDictionary

---

Item 51 - 精简initialize与load的实现代码

---

Item 52 - 别忘了NSTimer会保留其目标对象

---

Part C - Key Words

---

 消息结构 messaging structure * 函数调用 function calling * 虚方法表 virtual method table * 动态绑定 dynamic binding *
 运行期组件 runtime component * 动态库 dynamic library * 编译期 compile time * 超集 superset *
 内存模型 memory model * 引用计数 reference counting * 堆空间 heap stack * 栈空间 stack space *
 栈帧 stack frame * 非对象类型 nonobject type *

 头文件 header file * 实现文件 implemention file * 类 class * 基本头文件 base header file *
 使用者 consumer * 向前声明 forward declaring * 循环引用 chicken-and-egg situation * 委托协议 delegate protocol *
 class-continuation 分类 class-continuation category * 公共头文件 public header file * 耦合 coupling * 

 字符串字面量 string literal * 字面量语法 literal syntax * 取下标操作 subscripting * 语法糖 syntactic sugar *
 可变的 mutable * 非字面量语法 nonliteral syntax * 子族 class cluster * 不可变的 immutable *
 
 编译单元 translation unit * 命名习惯 naming convention * 名称空间 namespace * 目标文件 object file *
 外部符号 external symbol * 常值变量 constant variable * 全局符号表 global symbol table * 数据段 data section *
 只在编译单元内可见的常量 translation-unit-specific constant * 
 
 强类型 strong type * 枚举集 enumeration set * 套接字连接 socket connection * 底层数据类型 underlying type * 
 按位或操作符 bitwise OR operator * 按位与操作符 bitwise AND operator * 向后兼容 backward compatibility * 
 隐式转换 implicit cast * 显示转换 explicit cast * 状态机 state machine *

 对象 Object * 基本构造单元 building block * 消息传递 Messaging * Objective-C运行期环境 Objective-C runtime *
 
 属性 property * 存取方法 access method * 获取方法 getter * 设置方法 setter *
 点语法 dot syntax * 编译期 compile time * 偏移量 offset * 硬编码 hardcode *
 不兼容现象 incompatibility * 特殊变量 special variable * 类对象 class object * 
 应用程序二进制接口 Application Binary Interface * 稳固的 nonfragile * 
 自动合成 autosynthesis * 合成方法 synthesized method * 
 特质 attribute * 原子性 atomicity * 原子的 atomic * 读写 readwrite *
 只读 readonly * 具体的所有权语义 concrete ownership semantic * 
 保留 retain * 纯量类型 scalar type * 拥有关系 owning relationship * 非拥有关系 nonowning relationship *
 清空 nil out * 不保留 unretained * 不安全 unsafe * 拷贝 copy *
 初始化器 Initializer * 线程安全 thread safety * 

 方法派发 method dispatch * 键值观察 Key-Value Observing * 断点 breakpiont * 覆写 override *
 惰性初始化 lazy initialization * 
 
 等同性 equality * 不相等 unequal * 等同性判定方法 equality-checking method * 未定义 undefined * 
 指针值 pointer value * 约定 contract * 继承体系 inheritance hierarchy * 哈希码 hash *
 哈希表 hash table * 碰撞 collision * 强类型检查 strong type checking * 深度等同性判定 deep equality * 
 唯一标识符 unique identifier * 主键 primary key * 可变部分 mutable portion * 照原样 verbatim * 
 
 类族class cluster * 模式 pattern * 抽象基类 abstract base class * 用户界面框架 user interface framework *
 类方法 class method * 按钮类型 button type * 实体子类 concrete subclass * 工厂模式 Factory pattern *
 类型内省 type introspection * 占位数组 placeholder array * 内类型 internal type * 
 类型信息查询方法 introspection method * 工厂方法 factory method * 

 关联对象 Associated Object * 存储策略 storage policy * 不透明指针 opaque pointer * 块 block * 
 捕获 capture * 保留环 retain cycle * 正式的定义 formal definition * 

 传递消息 pass a message * 选择器 selector * 静态绑定 static binding * 内联 inline *
 动态绑定 dynamic binding * 接收者 receiver * 消息 message * 原型 prototype * 
 参数可变的函数 variadic function * 方法列表 list of methods * 消息转发 message forwarding *  快速映射表 fast map *
 快速执行路径 fast path * 静态绑定的函数调用操作 statically bound function call * 消息派发 message dispatch * 边界情况 edge case *
 浮点数寄存器 floating-point register * 尾调用优化 tail-call optimization * 栈踪迹 stack trace * 栈溢出 stack overflow *
 回溯 backtrace * 发送消息 invoke a message * 调用方法 call a method * 动态消息派发系统 dynamic message dispatch system *

 消息转发 message forwarding * 无缝桥接 toll-free bridging * 内部类 internal class * 未知选择子 unknown selector *
 动态方法解析 dynamic method resolution * 完整的消息转发机制 full forwarding mechanism * 
 备援接收者 replacement receiver * 组合 composition * 多重继承 multiple inheritance * 
 消息派发系统 message-dispatch system * 兼容于键值编码的 key-value-coding-compliant * 

 方法调配 method swizzling * 分类 category * 完全不知道其具体实现的 completely opaque * 

 方法签名 method signature * 类型信息查询 introspection * 公共根类 common root class * 指向 point to *
 元数据 metadata * 元类 metaclass * 类继承体系 class hierarchy * 动态类型系统 dynamic typing * 
 强类型的 strongly typed * 逗号分隔的字符串 comma-separated string * 单例 singleton * 代理 proxy *
 接受代理的对象 proxied object * 

 命名空间 naming clash * 动态加载器 dynamic loader * 重名符号错误 duplicate symbol error * 处置器 handler *
 两字母前缀 two-letter prefix * 顶级符号 top-level symbol * 回调函数 callback * 符号表 symbol table *
 
 全能初始化方法 designated initializer * 不一致的内部数据 inconsistent internal data * 
 序列化机制 serialization mechanism * 解压缩 unarchiving * 解码器 decoder * 
 完全遵守NSCoding协议 fully NSCoding compliant * 

 格式字符串 format string * 调试器 debugger * 对象打印 print-object * 

 只读 read-only * 既可读又可写 read-write * 可变的 mutable * 网络服务 web service * 
 可变对象 mutable object * 竞争条件 race condition * 派发队列 dispatch queue * hack 杂技代码 *
 爱用蛮力的 brutal * 列表 list * 解耦 decouple * 

 驼峰式大小写命名法 camel casing * 构造器 constructor * 工厂方法 factory method * 本地化字符串 localized string *
 输出参数 out-parameter * C语言式数组 C-style array * 视图 View * 文本框 text field *
 自明其意 self-explanatory * 试图控制器 view controller * 委托接口 delegate interface * 

 异常exception * 自动引用计数 Automatic Reference Counting * 异常安全的 exception safe * 
 致命错误 fatal error * 非致命错误 nonfatal error * 
 错误范围 Error domain * 处理URL的子系统 URL-handling subsystem * Error code 错误码 * 用户信息 User info *
 本地化描述 localized description * 错误链 chain of errors * 解引用 dereference * 段错误 segmentation fault *
 
 区 zone * 默认区 default zone * 副作用 side effect * 辅助方法 helper *
 可变版本 mutable variant * 不可变版本 immutable variant * 深拷贝 deep copy * 浅拷贝 shallow copy *
 
 ---

Chapter 4 - 协议与分类

---

 委托模式 Delegate pattern * 委托 delegate * 数据源 data source *
 以异步方式执行一项任务 perform a task asynchronously * 非拥有关系 nonowning relationship * 自动清空 autoniling *
 可选的 optional * 网络数据获取器 network fetcher * 重定向 redirect * 数据源模式 Data Source Pattern *
 列表视图 list view * 生命期 life cycle * 位段 bitfield * 数据源协议 data source protocol *

 主实现 main implementation * 功能区 function area * 符号名 symbol name * 自我描述式代码 self-documenting code *

 属性特质 attribute * 主接口 main interface * 键值观察 Key-Value Observing * 观察者 observer *
 竞争条件 race condition * 

 匿名对象 anonymous object * 匿名类 anonymous class * 匿名的 anonymous * 数据库连接 database connection *
 数据库处理器 database handler * 匿名类型 anonymous type * 内部状态对象 internal state object * 

---

Chapter 5 - 内存管理

---

 自动引用计数 Automatic Reference Counting * 垃圾收集器 garbage collector * 保留计数 retain count *
 引用计数 reference count * 自动释放池 autorelease pool * 回收 deallocated * 可重用 reuse *
 对象图 object graph * 强引用 strong reference * 拥有 own * 根对象 root object *
 可用内存池 available pool * 悬挂指针 dangling pointer * 强关系 strong relationship * 事件循环 event loop *
 方法调用边界 method call boundary * 保留环 retain cycle * 孤岛 island of isolation * 弱引用 weak reference * 

 静态分析器 static analyzer * 向后兼容性 backward compatibility * 边界情况 edge case *  
 按引用传递 pass by reference * 块 block * 保留环 retain cycle * 回收分配对象的内存 deallocate *
 清理代码 cleanup code * 清理例程 cleanup routine * 析构函数 destructor * 样板代码 boilerplate code *
 空操作 no-op * 

 观察行为 observation behavior * 订阅 register * 注销 unregister * 文件描述符 file descriptor *
 套接字 socket * 清理方法 cleanup method * 编程错误 programmer error * 已经尾声 in a winding-sown state *
 最终的释放操作 final release * 正在回收的状态 deallocating state * 

 异常 exception * 异常处理程序 exception handler * 析构函数 destructor * 异常处理例程 exception-handle routine *
 文件句柄 file handle * 

 环 cycle * 垃圾收集器 garbage collector * 非拥有关系 nonowning relationship * 无法安全使用 unsafe * 

 清空 drain * 大中枢派发 Grand Central Dispatch * 事件循环 event loop * 
 应用程序主入口点 main application entry point * 
 内存峰值 high-memory waterline * 最大内存用量 highest memory footprint * 
 重量级 heavyweight * 轻量级 lightweight * 
 
 僵尸对象 Zombie Object * 类列表 class list * 僵尸类 zombie class * 调配 swizzle * 
 完整的消息转发机制 full forwarding mechanism * 指针值 pointer value * 

 单例对象 singleton object * 编译器常量 compile-time constant * 标签指针 tagged pointer * 空操作 no-op *
 绝对保留计数 absolute retain count * 

---

Chapter 6 - 块与大中枢派发

---

 块 block * 大中枢派发 Grand Central Dispatch * 词法闭包 lexical closure * 环境 context * 
 派发队列 dispatch queue * 后台线程 background thread *
 只执行一次的线程安全代码 thread-safe single-code execution *

 扩展 extension * 内联块 inline block * 不透明的void指针 opaque void pointer * 
 分配在堆上的块 heap block * 分配在栈上的块 stack block * 全局块 global block * 

 固有类型 inherent type * 

---

Chapter 7 - 系统框架 System Framework

---

 动态库 dynamic library * 静态库 static library * 带图形界面的应用程序 graphical application * 
 无缝桥接 toll-free bridging * BSD套接字 BSD socket * 
 一等公民 first-class citizen * 社交网络 social networking *

 快速遍历 fast enumeration * 无序的 unordered * 具体子类 concrete subclass * 枚举器 enumerator *
 选项掩码 option mask * 按位或 bitwise OR * 

 无缝桥接 toll-free bridging * 桥式转换 bridged cast * 内存分配器 allocator * 

---

